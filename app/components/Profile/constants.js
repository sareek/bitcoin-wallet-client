// just a boilerplate for now. not in use.
// will be used for common stuff in the sub routes
// like updating redux state of userInfo and localStorage with changes, which is shared
// by all the sub routes

// this action is handled in login reducer
export const UPDATE_USER_INFO = "APP/Profile/UPDATE_USER_INFO";
